name: PR
on:
  pull_request_target:
    types:
      - opened
      - edited
      - ready_for_review
      - synchronize
  issue_comment:
    types: [created, edited]
jobs:
  labeler:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        uses: actions/setup-python@v1
        uses: BSFishy/pip-action@v1
        with:
          packages: |
            yq

      - name: Get Reviewers
        id: reviewers
        run: echo ::set-output name=reviewers::$(cat ./OWNERS | yq .reviewers)

      - name: Get Approvers
        id: approvers
        run: echo ::set-output name=approvers::$(cat ./OWNERS | yq .approvers)

      - name: Add Size label
        if: github.event_name == 'pull_request_target'
        uses: "pascalgn/size-label-action@v0.4.3"
        env:
          GITHUB_TOKEN: "${{ secrets.BOT_PAT }}"

      - name: Add WIP label / comment trigger
        if: |
          contains(github.event.pull_request.title, 'wip') ||
          (contains(github.event.comment.html_url, '/pull/') && 
          contains(github.event.comment.body, '/wip'))
        uses: andymckay/labeler@master
        with:
          repo-token: ${{ secrets.BOT_PAT }}
          add-labels: 'WIP'
